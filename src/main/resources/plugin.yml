name: ${project.artifactId}
main: ${project.groupId}.${project.name}.${project.artifactId}
version: ${project.version}
api-version: ${project.bukkitAPIVersion}
author: Goosius
website: 'https://github.com/TownyAdvanced/TownyResources'
prefix: ${project.artifactId}
depend: [Towny]

description: This is an add-on plugin for Towny, which provides towns and nations with valuable natural resources, creating a good reason for wars and trading.